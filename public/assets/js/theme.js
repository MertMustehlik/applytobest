$(window).on('load', function () {
    $(".page-loader").delay(800).fadeOut("slow");
    $(".loader-logo img").fadeOut(900);
});

/*$(document).ready(function(){
    if (localStorage['page'] == document.URL) {
        $(document).scrollTop(localStorage['scrollTop']);
    }
});*/

